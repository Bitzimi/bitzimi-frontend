name: Superseded transaction UI patch
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded; wallet transaction details are maintained by the active audit workflow."