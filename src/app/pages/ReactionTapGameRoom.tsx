// Reaction Tap's original UI/flow is canonical in app/.
// Keep src/app as the active route surface while reusing that original implementation.
export { default } from "../../../app/pages/ReactionTapGameRoom";
