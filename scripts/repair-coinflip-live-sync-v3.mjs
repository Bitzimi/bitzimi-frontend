import fs from 'node:fs';
const path='src/app/pages/PvPCoinFlipGame.tsx';
let s=fs.readFileSync(path,'utf8');
const marker='  const myUsername = identity.username;';
if(!s.includes('const [gameState, setGameState]') && s.includes(marker)){
  const block=`  const [feeRate,setFeeRate]=useState<number>(0);
  const [gameState,setGameState]=useState<GameState>("ready");
  const [coinResult,setCoinResult]=useState<CoinSide|null>(null);
  const [isWinner,setIsWinner]=useState(false);
  const [winAmount,setWinAmount]=useState(0);
  const [platformFee,setPlatformFee]=useState(0);
  const [showResultPopup,setShowResultPopup]=useState(false);
  const [sessionHistory,setSessionHistory]=useState<SessionRecord[]>([]);
  const playerAvatar=identity.avatar;
  const [opponentName,setOpponentName]=useState("");
  const [opponentAvatar,setOpponentAvatar]=useState("P");
  const [queueId,setQueueId]=useState<string|null>(null);
  const [matchId,setMatchId]=useState<string|null>(null);
  const [matchData,setMatchData]=useState<any>(null);
  const pollRef=useRef<ReturnType<typeof setInterval>|null>(null);
  const [winnerAvatar,setWinnerAvatar]=useState("");
  const [winnerName,setWinnerName]=useState("");
  const [showWinner,setShowWinner]=useState(false);
  const [playerSide,setPlayerSide]=useState<CoinSide|null>(null);
  const [opponentSide,setOpponentSide]=useState<CoinSide|null>(null);
  const [showRules,setShowRules]=useState(false);
  const [showFairness,setShowFairness]=useState(false);
  const [fairnessData,setFairnessData]=useState<FairnessData|null>(null);
  const [animationElapsedMs,setAnimationElapsedMs]=useState(0);
  const [animationDurationMs,setAnimationDurationMs]=useState(17000);
  const timersRef=useRef<ReturnType<typeof setTimeout>[]>([]);
  const searchInFlight=useRef(false);
  const transactionRecorded=useRef(false);
  const hasStarted=useRef(false);
  const clearTimers=()=>{for(const t of timersRef.current)clearTimeout(t);timersRef.current=[];};
  const clearPolling=()=>{if(pollRef.current){clearInterval(pollRef.current);pollRef.current=null;}};
  const loadHistory=async()=>{try{const rows=await gameMatchmakingService.getCoinFlipHistory(stakeAmount);setSessionHistory(rows.map((r)=>({id:r.matchId,opponent:r.opponent.username,opponentAvatar:r.opponent.avatar??null,result:r.youWon?"win":"loss",outcome:r.result??"heads",amount:r.youWon?r.payout-r.stake:r.stake,stake:r.stake,timestamp:r.settledAt??r.createdAt})));}catch{}};
  const applySettledResult=async(md:any)=>{const result=md?.result?.coinFlip as CoinSide|undefined;if(!result||md?.status!=="settled")return;const won=Boolean(md.youWon);const name=md.opponent?.username??"Player";const avatar=md.opponent?.avatar??name.charAt(0).toUpperCase();const winnings=Number(md.payout??0);setMatchData(md);setPlatformFee(Number(md.platformFee??0));setCoinResult(result);setIsWinner(won);setWinAmount(won?winnings:0);setWinnerAvatar(won?playerAvatar:avatar);setWinnerName(won?myUsername:name);setShowWinner(true);setGameState("showing_result");await refreshWalletsFromBackend().catch(()=>{});await loadHistory();if(!transactionRecorded.current){transactionRecorded.current=true;addGameResult({gameType:"pvp_coinflip",betAmount:stakeAmount,winAmount:won?winnings:0,profit:won?winnings-stakeAmount:-stakeAmount,won,opponent:name,outcome:result});}setShowResultPopup(true);};
  const syncActiveMatch=async(id:string)=>{try{const md=await gameMatchmakingService.getMatch(id);setMatchData(md);setOpponentName(md.opponent?.username??"Player");setOpponentAvatar(md.opponent?.avatar??md.opponent?.username?.charAt(0).toUpperCase()??"P");setPlayerSide(md.playerSide??(md.isPlayer1?md.result?.p1Side:md.result?.p2Side)??null);setOpponentSide(md.opponentSide??(md.isPlayer1?md.result?.p2Side:md.result?.p1Side)??null);setAnimationDurationMs(Number(md.animationDurationMs??17000));setAnimationElapsedMs(Number(md.animationElapsedMs??0));if(md.status==="settled"){clearPolling();await applySettledResult(md);return true;}setGameState(md.phase==="flipping"?"flipping":"side_assignment");return false;}catch{return false;}};
  const waitForSettlement=(id:string)=>{clearPolling();pollRef.current=setInterval(()=>{syncActiveMatch(id);},250);syncActiveMatch(id);};
  const startSearch=async()=>{if(searchInFlight.current||(gameState!=="ready"&&gameState!=="idle"))return;if(balances.game<stakeAmount){toast.error("Insufficient balance in Game Wallet");return;}searchInFlight.current=true;clearPolling();clearTimers();transactionRecorded.current=false;setShowResultPopup(false);setShowWinner(false);setCoinResult(null);setMatchId(null);setMatchData(null);setQueueId(null);setPlayerSide(null);setOpponentSide(null);setAnimationElapsedMs(0);setAnimationDurationMs(17000);setGameState("searching");try{if(privateMatchId){const match=await gameMatchmakingService.getMatch(privateMatchId);setMatchId(privateMatchId);setMatchData(match);setOpponentName(match.opponent.username);setOpponentAvatar(match.opponent.avatar??match.opponent.username.charAt(0).toUpperCase());await refreshWalletsFromBackend().catch(()=>{});assignSides(match);return;}const result=await gameMatchmakingService.joinQueue("pvp_coinflip",stakeAmount);if(result.status==="matched"&&result.matchId){const match=await gameMatchmakingService.getMatch(result.matchId);setMatchId(result.matchId);setMatchData(match);setOpponentName(match.opponent.username);setOpponentAvatar(match.opponent.avatar??match.opponent.username.charAt(0).toUpperCase());await refreshWalletsFromBackend().catch(()=>{});assignSides(match);return;}if(result.queueId){setQueueId(result.queueId);pollRef.current=setInterval(async()=>{try{const status=await gameMatchmakingService.pollQueue(result.queueId!);if(status.status==="matched"&&status.matchId){clearPolling();const match=await gameMatchmakingService.getMatch(status.matchId);setMatchId(status.matchId);setMatchData(match);setOpponentName(match.opponent.username);setOpponentAvatar(match.opponent.avatar??match.opponent.username.charAt(0).toUpperCase());await refreshWalletsFromBackend().catch(()=>{});assignSides(match);}else if(status.status==="cancelled"){clearPolling();searchInFlight.current=false;setGameState("ready");}}catch{}},250);}}catch{searchInFlight.current=false;setGameState("ready");toast.error("Unable to start matchmaking");}};
`;
  s=s.replace(marker,marker+'\n'+block);
}
s=s.replace('  useEffect(() => {  useEffect(() => {','  useEffect(() => {');
fs.writeFileSync(path,s);
console.log('Coin Flip live-sync state declarations restored');
