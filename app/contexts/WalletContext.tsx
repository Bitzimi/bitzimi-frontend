// Compatibility bridge: the active application provider lives under src/app/contexts.
// Legacy app/ components therefore consume the same WalletContext instance.
export * from "../../src/app/contexts/WalletContext";
